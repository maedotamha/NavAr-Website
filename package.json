{
  "name": "navar-platform",
  "version": "1.0.0",
  "private": true,
  "description": "NavAR platform with Node/PostgreSQL backend and Next.js frontend.",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "node scripts/start.js",
    "db:init": "npm run db:init --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "check": "npm run check --prefix backend"
  },
  "keywords": ["navar", "nextjs", "postgresql", "postgis", "indoor-navigation"]
}